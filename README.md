# DistractionBlocker
Embedded site blocker that is wired to FastAPI backend, Supabase handles API routes, and the chrome extension runs and executes the background file to prevent users from accessing blocked websites.
